from project_timer.combiner import Combiner

if __name__ == "__main__":
    # name = input("Username:")
    # id = input("Employee ID")

    name = "Chad"
    id = 234
    A = Combiner(name, id)
    print(A.usr())
